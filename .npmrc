# Security configuration for npm
audit-level=moderate
fund=false
save-exact=true
package-lock=true

# Prevent running scripts as root
unsafe-perm=false

# Use latest npm registry with security patches
registry=https://registry.npmjs.org/

# Security audit settings
audit=true